{
  "name": "astri-web-portfolio-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app install && npm --prefix app run build",
    "preview": "npm --prefix app run preview"
  }
}
